export function tokenConfig(tokenPar) {
  const token = tokenPar;

  const config = {
    headers: {
      "Content-type": "application/json",
    },
  };

  if (token) {
    config.headers["x-auth-token"] = token;
  }

  return config;
}
